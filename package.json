{
  "name": "immutee",
  "version": "1.0.0",
  "description": "Simple utility for changing object without mutate it.",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/diruuu/immutee.git"
  },
  "scripts": {
    "start": "node test.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --presets babel-preset-es2015 --out-dir dist",
    "prepublish": "npm run build"
  },
  "author": "Rudi Wahyudi",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-es2015": "^6.22.0"
  }
}
